[project]
name = "identity-generator"
version = "0.0.2"
description = "A Chinese identity card generator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.0",
    "gevent>=24.0",
    "webargs>=8.0",
]

[dependency-groups]
dev = [
    "requests>=2.32",
]

[tool.uv]
package = false
